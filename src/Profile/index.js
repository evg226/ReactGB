import { Typography } from '@material-ui/core/';

function Profile() {
    return (
        <div className="container">
            <Typography variant="h4" className="page-header" gutterBottom>Профиль пользователя</Typography>
        </div>
    )
}

export default Profile;